[build]
  base    = "frontend"
  command = "yarn build"
  publish = "build"

# SPA fallback — every unmatched path serves index.html so React Router can route it
[[redirects]]
  from   = "/*"
  to    = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
